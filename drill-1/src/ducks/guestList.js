// Our reducer: function, takes in state and action. It needs to return state, if modified, it returns a new piece of state.

const initialState = [];

export default function reducer(state = initialState, action){
  return state;
}